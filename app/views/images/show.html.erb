
<div class="show-cat">
  <%= image_tag @image.photo(:medium) %>
  <h2 class="caption">"<%= @image.caption %>"</h2>
</div>

<% if current_user %>
  <%= render "tags/new" %>

  <% if @image.tags.any? %>
    <% @image.tags.each do |tag| %>
    <%= button_to tag.user.email, image_tag_path(tag.image, tag), :method =>"delete" %>
    <% end %>
  <% end %>
  <% else %>
  <p>Joint to tag photos</p>
<% end %>

<p>
<% if current_user == nil %>
  <p>Reprimand: You Should Sign Up To Katstagram!</p>
<% else %>
  <% if current_user.id == @image.user_id %>
    <%= link_to "Edit", edit_image_path(@image) %> <br>
    <%= link_to "Delete", image_path(@image),
                             :data => {:confirm => "You sure?",
                                       :method => "delete"} %>
  <% else %>
    <%= "You are not authorized to Edit or Delete this image"%>
  <% end %>
<% end %>
</p>
